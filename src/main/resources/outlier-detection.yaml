#true use redis spout, else use random spout
#default is false
spout.redis: false

spout.parallelism: 1
spout.object.size: 20

projection.parallelism: 1
projection.dimension: 40
projection.size: 20

detector.neighbor.distance.max: 10
detector.neighbor.count.min: 30
detector.parallelism: 1

updater.parallelism: 1

#set worker count
topology.workers: 1

##redis config if redis spout is enabled
redis.host: "localhost"
redis.port: 6379
redis.queue: "vectors"